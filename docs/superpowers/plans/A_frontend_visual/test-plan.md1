# 阶段A测试计划（前端可视化）

## 1. 范围与目标
- 验证 React 前端是否满足商品管理可视化与定时配置占位需求。
- 验证默认路由、侧边栏常驻布局、每页 15 条分页、批量操作与异常处理。

## 2. 功能用例
| 编号 | 场景 | 操作 | 预期结果 |
|---|---|---|---|
| A-01 | 默认首页 | 访问 `http://localhost:3000` | 默认进入商品管理页 |
| A-02 | 侧边栏路由 | 点击“商品管理/定时配置” | 右侧内容区切换，左侧栏常驻不消失 |
| A-03 | 未知路由 | 访问不存在路径 | 自动重定向到 `/products` |
| A-04 | 单条新增 | 填写 platform/url/title/active 提交 | 新增成功并刷新列表 |
| A-05 | 单条编辑 | 修改标题或 active | 修改成功并在列表可见 |
| A-06 | 单条删除 | 点击删除并确认 | 删除成功，列表中消失 |
| A-07 | 批量新增 | 粘贴多条 URL 执行导入 | 逐条创建，结果反馈成功/失败数量 |
| A-08 | 批量删除 | 多选后批量删除 | 选中项删除成功，结果汇总展示 |
| A-09 | 批量启停 | 多选后批量启用/停用 | 对应商品 active 状态更新 |
| A-10 | 筛选查询 | 使用 platform/active/关键词筛选 | 列表按条件过滤 |
| A-11 | 分页 | 触发超过 15 条数据并翻页 | 每页 15 条，翻页结果正确 |
| A-12 | Cron 占位 | 输入合法 5 段 cron 并保存 | 保存到 localStorage（`crawl_cron_draft`）成功 |
| A-13 | Cron 校验 | 输入非法 cron | 阻止保存并提示错误 |

## 3. 边界与异常用例
| 编号 | 场景 | 预期结果 |
|---|---|---|
| A-E01 | 列表为空 | 显示空态，不报错 |
| A-E02 | 首次加载慢 | 显示 loading 态，完成后恢复 |
| A-E03 | URL 非法或无法识别平台 | 阻止提交并给出可读提示 |
| A-E04 | 批量新增存在重复 URL | 输入内去重，且与已有数据重复时提示 |
| A-E05 | 批量操作部分失败 | 显示成功/失败统计与失败原因 |
| A-E06 | 删除操作 | 必须二次确认后执行 |
| A-E07 | 翻页后多选 | 多选作用域明确（建议仅当前页）且 UI 有提示 |
| A-E08 | 接口超时或 5xx | 显示失败提示并给出重试入口 |

## 4. 验收标准
- `A-01` 至 `A-13` 全部通过。
- 边界用例中无“静默失败”与“无反馈错误”。
- 用户能独立完成：增删改查、批量处理、分页浏览、Cron 草稿保存。

